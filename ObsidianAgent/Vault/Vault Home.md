---
project: ObsidianAgent
type: dashboard
tags:
  - obsidian-agent
  - vault
  - dashboard
---

# Vault Home

Meta-vault entrypoint for shared project context.

## Start Here
- [[README]]
- [[Repository Map]]

## Projects
- Review generated project indexes under `Vault/Generated/`.
- Open repo-local vaults inside `Projects/` when deeper project context is needed.

## Working Rules
- Run the shared Obsidian agent with `--check` before `--sync`.
- Keep project-specific vault contracts inside each project repo.
- Use this vault for cross-project context, not to duplicate project source truth.
