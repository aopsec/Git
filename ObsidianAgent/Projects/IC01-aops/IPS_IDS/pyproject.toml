[tool.ruff]
line-length = 100
target-version = "py314"
extend-exclude = [
  ".vendor",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "N", "SIM"]

[tool.mypy]
python_version = "3.14"
strict = true
exclude = [
  "^\\.vendor/",
]
