# /etc/fail2ban/filter.d/caddy-openbox-auth.conf
# [FIX-AUDIT-F2B] Match Caddy JSON access-log entries for HTTP auth failures.

[Definition]
failregex = ^.*"remote_ip":"<HOST>".*"status":401(?:,|}).*$
ignoreregex =
